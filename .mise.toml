[tasks.setup]
description = "Setup skills symlinks using stow"
run = "stow -v -t ~ claude-code-skills"

[tasks.teardown]
description = "Remove skills symlinks"
run = "stow -v -D -t ~ claude-code-skills"

[tasks.restow]
description = "Restow skills (remove and re-create symlinks)"
run = "stow -v -R -t ~ claude-code-skills"
